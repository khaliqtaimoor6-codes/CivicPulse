apiVersion: v1
kind: Service
metadata:
  name: civicpulse-frontend
  namespace: civicpulse
  labels:
    app: civicpulse-frontend
spec:
  type: ClusterIP
  selector:
    app: civicpulse-frontend
  ports:
    - name: http
      port: 80
      targetPort: http