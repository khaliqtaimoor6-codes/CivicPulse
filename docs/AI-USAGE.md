# AI-USAGE

- Copilot, backend scaffold: kept the SQLAlchemy model, Alembic migration, settings loader, triage protocol/rules, repository, service, and seed implementation; changed enum collision handling, transaction commits, and migration enum creation after executable checks exposed them.
- Copilot, frontend scaffold: kept the Vite project structure and Docker assets; changed the starter app into routed Submit/Dashboard/Stats pages, added contract TypeScript types, static form validation, Vite proxy, and React Router v6.
- Copilot, integration verification: kept the Compose topology; changed YAML indentation and added the minimal backend image needed for a successful four-service build, then verified running services and API/OpenAPI responses.
- Copilot, local LLM triage: wrote `OllamaTriage` as a new provider against the existing `TriageProvider` protocol. It kept the strict key-set and Pydantic validation pattern from `llm.py` and the timeout-and-fallback behaviour of `TriageService`; it replaced the abstract enum-listing prompt with a category-defining prompt and the soft `format: "json"` hint with a real JSON schema after measurement showed 0/6 responses were otherwise rejected. `llm:groq` remains the default recommendation; the 1B local model measures 90% category but 33% priority accuracy, documented in `docs/TRIAGE.md`.
